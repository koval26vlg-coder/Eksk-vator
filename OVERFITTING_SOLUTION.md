# Решение проблемы Overfitting (переоптимизации)

**Дата**: 2026-04-27  
**Проблема**: 100+ параметров, 11 стратегий → подгонка под историю, не работает на будущем

---

## Текущая ситуация (ПЛОХО)

```
Стратегии: 11 вариантов
├─ momentum, filtered_momentum, mean_reversion
├─ ta_conservative, ta_aggressive, ta_trend, ta_regime
├─ adaptive, rotate, ta_rotate, orderflow
└─ Каждая со своими параметрами

Параметров: 100+
├─ TA: RSI, ADX, BB, SMA, ATR, VWAP, VP (20+ параметров)
├─ Entry: impulse, range, spread, ATR filters (15+ параметров)
├─ Exit: TP, SL, EARLY_STOP, MID_STOP, MAX_HOLD (25+ параметров)
├─ Risk: notional, limits, cooldowns (15+ параметров)
└─ Volatility: sigma, vol-scale, spike detection (10+ параметров)

Проблема:
├─ Нужно 3,000+ сделок для валидации
├─ За месяц: 60-150 сделок (swing)
└─ Покрывает только 2-5 параметров из 100
```

---

## Решение 1: МИНИМАЛЬНАЯ стратегия (РЕКОМЕНДУЕТСЯ)

### Концепция: "Trend Following 101"

**Одна идея**: Покупай на восходящем тренде, продавай на нисходящем

**Индикаторы** (всего 3):
1. **SMA(20)** — направление тренда (цена выше = uptrend)
2. **ADX(14)** — сила тренда (ADX > 20 = есть тренд)
3. **ATR(14)** — волатильность для SL

**Параметры** (всего 8):
```bash
# === ENTRY (3 параметра) ===
TA_SMA_PERIOD=20              # Тренд
TA_ADX_PERIOD=14              # Сила
TA_TREND_ADX_TRIGGER=20       # Порог силы

# === EXIT (3 параметра) ===
AUTO_TRADE_TP_BPS=70          # Take profit
AUTO_TRADE_SL_BPS=100         # Stop loss (фиксированный)
AUTO_TRADE_SL_ATR_MULT=2.0    # SL адаптивный (max из двух)

# === RISK (2 параметра) ===
AUTO_TRADE_NOTIONAL=80        # Размер позиции
AUTO_TRADE_MAX_OPEN_ORDERS=1  # Одна позиция
```

**Правила входа**:
```python
LONG:
  - Цена > SMA(20)           # Uptrend
  - ADX > 20                 # Есть сила
  - Нет открытых позиций     # Одна позиция

SHORT:
  - Цена < SMA(20)           # Downtrend
  - ADX > 20                 # Есть сила
  - Нет открытых позиций     # Одна позиция
```

**Правила выхода**:
```python
EXIT:
  - TP: +70 bps от входа
  - SL: max(100 bps, 2.0 × ATR) от входа
  - MAX_HOLD: 12 часов (аварийный)
```

### Преимущества минимальной стратегии

✅ **8 параметров** вместо 100+  
✅ Нужно **240 сделок** для валидации (не 3,000)  
✅ За **2 месяца** swing получите достаточно данных  
✅ Легко понять, что работает и что нет  
✅ Меньше риск переоптимизации  

### Что УБРАТЬ из .env

```bash
# УБРАТЬ все фильтры входа (переоптимизация):
# SCALPING_AUTO_TRADE_MIN_IMPULSE_BPS=30      # УБРАТЬ
# SCALPING_MIN_MID_RANGE_BPS=35               # УБРАТЬ
# TA_RSI_OVERSOLD=30                          # УБРАТЬ
# TA_RSI_OVERBOUGHT=70                        # УБРАТЬ
# TA_TREND_MIN_DI_DIFF=3                      # УБРАТЬ
# TA_TREND_MAX_RSI_LONG=75                    # УБРАТЬ
# TA_TREND_MAX_EXTEND_BPS_LONG=50             # УБРАТЬ
# AUTO_TRADE_ATR_MAX_SPREAD_MULT=0.5          # УБРАТЬ
# SCALPING_SIGMA_SPIKE_ABS_BPS=100            # УБРАТЬ

# УБРАТЬ сложные выходы (оставить только TP/SL):
# AUTO_TRADE_EARLY_STOP_SECONDS=900           # УБРАТЬ
# AUTO_TRADE_MID_STOP_START_SECONDS=3600      # УБРАТЬ
# AUTO_TRADE_MAX_HOLD_MIN_PNL_BPS=18          # УБРАТЬ
# AUTO_TRADE_MAX_HOLD_SKIP_IF_PNL_NET_GE_NEG_BPS  # УБРАТЬ

# Оставить только аварийный MAX_HOLD:
AUTO_TRADE_MAX_HOLD_HARD_SECONDS=43200  # 12 часов
```

---

## Решение 2: Walk-Forward тестирование (для продвинутых)

Если хотите оставить сложную стратегию, нужна **правильная валидация**:

### Метод Walk-Forward

```
История: 12 месяцев данных
├─ Месяц 1-3: обучение (подбор параметров)
├─ Месяц 4: тест (торговля с параметрами из 1-3)
├─ Месяц 2-4: обучение (пересчёт параметров)
├─ Месяц 5: тест (торговля с новыми параметрами)
└─ ... повторять

Если стратегия работает на ВСЕХ тестовых месяцах:
  → Параметры не переоптимизированы
Если работает только на обучении:
  → Overfitting, стратегия не работает
```

**Проблема**: Нужны исторические данные за 12+ месяцев и код для бэктеста

---

## Решение 3: Ансамбль простых стратегий (компромисс)

Вместо одной сложной — несколько простых:

### Стратегия A: Trend (SMA + ADX)
```python
LONG: price > SMA(20) AND ADX > 20
EXIT: TP=70 bps OR SL=100 bps
```

### Стратегия B: Breakout (BB)
```python
LONG: price касается нижней BB(20,2) AND RSI < 35
EXIT: TP=70 bps OR SL=100 bps
```

### Стратегия C: Momentum (ROC)
```python
LONG: ROC(10) > 0.5% AND volume > avg(20)
EXIT: TP=70 bps OR SL=100 bps
```

**Правило**: Торговать только когда **2 из 3** стратегий дают сигнал в одну сторону

**Параметров**: 3 стратегии × 5 параметров = 15 (не 100)  
**Нужно сделок**: 15 × 30 = 450 (не 3,000)  
**Время валидации**: 3-4 месяца swing (не 2-4 года)

---

## Что делать СЕЙЧАС (пошагово)

### Шаг 1: Упростить до минимума (сегодня)

Создать `.env.minimal` с 8 параметрами:

```bash
# === CORE ===
BOT_STRATEGY=scalping
SCALPING_VARIANT=ta_trend
DATA_MODE=ws
TA_TIMEFRAME=15m

# === ENTRY (3 параметра) ===
TA_SMA_PERIOD=20
TA_ADX_PERIOD=14
TA_TREND_ADX_TRIGGER=20

# === EXIT (3 параметра) ===
AUTO_TRADE_TP_BPS=70
AUTO_TRADE_SL_BPS=100
AUTO_TRADE_SL_ATR_MULT=2.0
AUTO_TRADE_MAX_HOLD_HARD_SECONDS=43200

# === RISK (2 параметра) ===
AUTO_TRADE_NOTIONAL=80
AUTO_TRADE_MAX_OPEN_ORDERS=1
RISK_MAX_TOTAL_NOTIONAL=250

# === УБРАТЬ ВСЕ ФИЛЬТРЫ ===
# Без MIN_IMPULSE, MIN_RANGE, RSI filters, DI filters, ATR filters
# Без EARLY_STOP, MID_STOP, MIN_PNL
# Только: SMA + ADX → вход, TP/SL → выход
```

### Шаг 2: Paper test 2 месяца (апрель-июнь)

```
Цель: 120-300 сделок
├─ Достаточно для валидации 8 параметров
├─ Смотреть: win rate, profit factor, drawdown
└─ НЕ подкручивать параметры каждую неделю!
```

### Шаг 3: Анализ результатов

**Если прибыльно** (profit factor > 1.5):
  → Стратегия работает, можно добавить 1-2 фильтра

**Если убыточно** (profit factor < 1.2):
  → Проблема в идее, не в параметрах
  → Попробовать другую простую идею (breakout, mean reversion)

### Шаг 4: Добавлять сложность ПОСТЕПЕННО

```
Месяц 1-2: SMA + ADX (8 параметров)
  ↓ Если работает
Месяц 3-4: + RSI filter (10 параметров)
  ↓ Если улучшает результат
Месяц 5-6: + ATR spread filter (12 параметров)
  ↓ Если улучшает результат
...

НЕ добавлять всё сразу!
```

---

## Красные флаги overfitting

🚩 **Параметры подбирались вручную** под историю  
🚩 **Стратегия работает на прошлом месяце, не работает на текущем**  
🚩 **Больше 20 параметров** без walk-forward теста  
🚩 **"Если добавить ещё один фильтр, будет +10%"** — это overfitting  
🚩 **Меняете параметры каждую неделю** — гонка за прошлым  

---

## Зелёные флаги робастности

✅ **Стратегия работает на разных парах** (BTC, ETH, BNB)  
✅ **Стратегия работает на разных таймфреймах** (15m, 30m, 1h)  
✅ **Параметры "круглые"** (20, 50, 100 — не 23.7, 48.3, 97.1)  
✅ **Результат стабилен при ±20% изменении параметров**  
✅ **Простая логика**, которую можно объяснить за 30 секунд  

---

## Итоговая рекомендация

### Для вас (сейчас):

1. **Создать `.env.minimal`** с 8 параметрами (SMA + ADX + TP/SL)
2. **Paper test 2 месяца** без изменений параметров
3. **Если profit factor > 1.5** → добавить 1-2 фильтра
4. **Если profit factor < 1.2** → попробовать другую простую идею

### НЕ делать:

❌ Оставлять 100+ параметров "на всякий случай"  
❌ Подкручивать параметры каждую неделю под результаты  
❌ Добавлять фильтры, чтобы "исправить" убыточные сделки  
❌ Верить бэктесту без walk-forward валидации  

---

## Цитата от профессионалов

> "The best trading strategies are so simple, you can explain them to a 10-year-old.  
> If you need 100 parameters, you don't have a strategy — you have a curve-fit."  
>  
> — Larry Williams, трейдер с 50+ годами опыта

---

**Следующий шаг**: Создать `.env.minimal` и запустить 2-месячный paper test?
