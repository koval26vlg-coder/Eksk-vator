# HANDOFF — передача контекста между сессиями

Краткие решения и **«не делать X»**, чтобы не повторять отклонённые шаги. Файл **в git**: не пишите сюда секреты, ключи API и значения из приватного `.env` / `secrets.env`.

Полный чеклист источников контекста (git, логи, скрипт, транскрипты, GitHub) — **`AGENTS.md`**, раздел **«5. Контекст между сессиями»**.

## Как вести записи

- Добавляйте **новые записи сверху** (сразу под этой секцией «Записи»).
- Одна мысль — коротко: дата, что решили, почему; или явно: «**Не делаем** … — потому что …».
- Ссылки: `Issue #N`, URL GitHub, или в коммите `Refs #N` / `Fixes #N`.

## Записи

<!-- новые строки — сюда, сверху -->

- **2026-04-23:** `quality` теперь допускает **2 позиции в одном направлении** (`AUTO_TRADE_MAX_POSITIONS_SAME_DIRECTION=2`) и возвращён **мягкий MAX_HOLD=1800s** + **HARD=7200s** (чтобы позиции не «залипали» без выхода).
- **2026-04-23:** из `quality` убран **SOL** (дефолтные пары теперь `BTC/USDT,BNB/USDT`).
- **2026-04-23:** добавлен **anti‑reentry cooldown** (после выхода запрещён быстрый повторный вход в ту же сторону; есть override для SOL, но SOL в `quality` сейчас убран).
- **2026-04-23:** WS shutdown fix: фоновые задачи в `ws_stream.py` корректно отменяются/дожидаются на остановке, чтобы уменьшить `Unclosed client session`.
- **2026-04-19 (после разбора run-145124):** в `quality` — **MAX_HOLD 1800 s**, **single-open** (paper, одна позиция на все пары), чуть **шире SL** при включённом SL + **SL_ATR_MULT≥1.25**; цель: меньше таймерных минусов (BTC+ETH одновременно) и реже вынос по шуму при том же TP net.
- **2026-04-19:** профиль `quality` в `config.py` ужесточён (импульс/DI/ATR×spread, AUTO_TUNE thr, мин. кулдаун 45 с) — цель: **реже входы, плотнее по качеству**; смотреть логи `quiet_market_flat_auto` и частоту сделок.
